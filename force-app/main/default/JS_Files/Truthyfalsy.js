var arr = [];
if(arr)
{
    console.log(`[] is truthy is JS`);   //Print: [] is truthy is JS
}

//always check length

if(arr.length === 0){
    console.log(`[] is having length = ${arr.length}`);  //[] is having length = 0
}